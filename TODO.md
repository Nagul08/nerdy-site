# Portfolio Backlog & Future Tasks

## Scheduled for v2.1
- [ ] **GitHub Heatmap Overhaul (`~/github`)**:
  - Integrate live GitHub GraphQL or REST contributions API with caching (e.g. via serverless function or static workflow).
  - High-res SVG activity graph with responsive tooltips matching the MGS2 / Tokyo Night terminal aesthetic.
  - Re-enable the `~/github` tab in `TerminalHeader` and un-comment `<GitHubSection />` in `App.tsx`.

## Completed in v2.0
- [x] Metal Gear Solid 2 (MGS2) Codec communication interface styling on CLiAMP streamer.
- [x] 20-segment tactical LED volume ladder with native wheel scrolling (`passive: false`) and pointer drag.
- [x] Smooth scroll alignment accounting for sticky header offset across all sections.
- [x] Removal of all personal location data (`Chennai, India` / `chennai-in`).
- [x] Custom Dante image display box with tactical MGS2 corner reticles.
- [x] Official name "Siva Kowsik S" prioritized with "Nagul" as nickname.
